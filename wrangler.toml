name = "gh-stats"
main = "src/index.ts"
compatibility_date = "2024-11-27"

[vars]
# GITHUB_USERNAME = "your-username"  # Set via wrangler secret or dashboard

# Secrets (set via `wrangler secret put`):
# - GITHUB_TOKEN
# - GITHUB_USERNAME (if you prefer it as a secret)
